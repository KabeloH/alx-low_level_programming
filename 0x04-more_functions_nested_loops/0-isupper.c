#include <stdio.h>
/**
 *A function that checks for uppercase character.
 *
 *Return 1 if a is uppercase else 0
 */
int _isupper(int a)
{
	if (isupper(a))
	putchar('1');
	else
	putchar('0');
}
